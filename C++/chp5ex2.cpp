#include <iostream>
#include <math.h>
using namespace std;

int main(){
    int age = 17;

    if (age > 16 && age < 65) {
        cout << "Age is within the normal working age." << endl;
    }
    return 0;
}